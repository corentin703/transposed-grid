import {defineCustomElements} from '../loader';

defineCustomElements();

import '../dist/transposed-grid/transposed-grid.css'

export const parameters = {
  actions: { argTypesRegex: "^on[A-Z].*" },
  controls: {
    matchers: {
      color: /(background|color)$/i,
      date: /Date$/,
    },
  },
}
